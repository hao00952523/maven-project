pipeline {
    agent any
    tools{
        maven 'local maven'
    }
    
    stages{
        stage('Build'){
            steps {
                //sh 'mvn clean package'
                echo '123456'
            }
            post {
                success {
                    echo 'saveing...'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
            }
        }
    }
}