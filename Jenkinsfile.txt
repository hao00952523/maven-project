pipeline {
    agent any
    tools{
        maven 'local maven'
    }
    
    stages{
        stage('Build'){
            steps {
                sh '''
                    echo "PATH = ${PATH}"
                    echo "M2_HOME = ${M2_HOME}"
                '''
            }
            post {
                success {
                    echo 'saveing...'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
            }
        }
    }
}